---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';

const services = await getCollection('services');
const sortedServices = services.sort((a, b) => (a.data.order || 0) - (b.data.order || 0));
---

<!doctype html>
<html lang="ar" dir="rtl">
	<head>
		<BaseHead title={`الخدمات | ${SITE_TITLE}`} description="خدماتنا المميزة" />
		<style>
			.page-hero {
				background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
				color: white;
				padding: 4rem 2rem;
				text-align: center;
				margin: -3em -1em 0;
			}
			.page-hero h1 {
				color: white;
				font-size: 2.5rem;
				margin-bottom: 0.5rem;
			}
			.page-hero p {
				opacity: 0.9;
				font-size: 1.1rem;
			}
			.services-grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
				gap: 2rem;
				padding: 3rem 0;
			}
			.service-card {
				background: white;
				border-radius: 20px;
				padding: 2.5rem;
				box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
				transition: all 0.3s ease;
				border: 1px solid #eee;
				position: relative;
				overflow: hidden;
			}
			.service-card::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				height: 4px;
				background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
			}
			.service-card:hover {
				transform: translateY(-8px);
				box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
			}
			.service-icon {
				font-size: 4rem;
				margin-bottom: 1.5rem;
				display: block;
			}
			.service-card h2 {
				margin: 0 0 1rem 0;
				font-size: 1.5rem;
				color: #333;
			}
			.service-card p {
				color: #666;
				margin: 0 0 1.5rem 0;
				line-height: 1.7;
			}
			.service-price {
				font-weight: bold;
				color: #667eea;
				font-size: 1.5rem;
				margin-bottom: 1rem;
			}
			.service-link {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				color: #667eea;
				text-decoration: none;
				font-weight: bold;
				transition: gap 0.3s ease;
			}
			.service-link:hover {
				gap: 1rem;
			}
			.featured-badge {
				position: absolute;
				top: 1.5rem;
				left: 1.5rem;
				background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
				color: white;
				padding: 0.25rem 0.75rem;
				border-radius: 20px;
				font-size: 0.8rem;
				font-weight: bold;
			}
			.empty-state {
				text-align: center;
				padding: 4rem 2rem;
				color: #666;
			}
			main {
				width: 1100px;
				max-width: calc(100% - 2rem);
				margin: 0 auto;
				padding: 2rem 1rem;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="page-hero">
				<h1>💼 خدماتنا</h1>
				<p>نقدم مجموعة متكاملة من الخدمات الرقمية الاحترافية</p>
			</section>
			
			{sortedServices.length > 0 ? (
				<div class="services-grid">
					{sortedServices.map((service) => (
						<article class="service-card">
							{service.data.featured && <span class="featured-badge">⭐ مميز</span>}
							<span class="service-icon">{service.data.icon || '⭐'}</span>
							<h2>{service.data.title}</h2>
							<p>{service.data.description}</p>
							{service.data.price && (
								<div class="service-price">{service.data.price}</div>
							)}
							<a href={`/services/${service.slug}`} class="service-link">
								اطلب الآن <span>←</span>
							</a>
						</article>
					))}
				</div>
			) : (
				<div class="services-grid">
					<article class="service-card">
						<span class="featured-badge">⭐ مميز</span>
						<span class="service-icon">🌐</span>
						<h2>تصميم مواقع</h2>
						<p>نصمم مواقع احترافية وسريعة ومتوافقة مع جميع الأجهزة مع SEO عالي</p>
						<div class="service-price">من 500$</div>
						<a href="/contact" class="service-link">اطلب الآن <span>←</span></a>
					</article>
					<article class="service-card">
						<span class="featured-badge">⭐ مميز</span>
						<span class="service-icon">📱</span>
						<h2>تطوير تطبيقات</h2>
						<p>نطور تطبيقات موبايل iOS و Android بأحدث التقنيات والأداء العالي</p>
						<div class="service-price">من 1000$</div>
						<a href="/contact" class="service-link">اطلب الآن <span>←</span></a>
					</article>
					<article class="service-card">
						<span class="service-icon">🎨</span>
						<h2>تصميم جرافيك</h2>
						<p>نصمم هوية بصرية كاملة وتصاميم سوشيال ميديا جذابة</p>
						<div class="service-price">من 200$</div>
						<a href="/contact" class="service-link">اطلب الآن <span>←</span></a>
					</article>
					<article class="service-card">
						<span class="service-icon">🔍</span>
						<h2>تحسين SEO</h2>
						<p>نحسن ترتيب موقعك في محركات البحث وزيادة الزيارات</p>
						<div class="service-price">من 300$</div>
						<a href="/contact" class="service-link">اطلب الآن <span>←</span></a>
					</article>
				</div>
			)}
		</main>
		<Footer />
	</body>
</html>
